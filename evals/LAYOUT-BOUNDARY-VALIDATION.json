{
  "schema": "agent-sdlc/eval-report/v1",
  "suite": "layout-boundary",
  "authority": "runtime/layout.mjs",
  "scanned_files": 205,
  "allowlist": [
    {
      "file": "scripts/test-layout-boundary.mjs",
      "reason": "this file: the self-check must contain a sample of every pattern to prove the pattern still fires, and a rule that cannot be shown to fail is a rule nobody should trust"
    },
    {
      "file": "adapters/hooks/statusline.mjs",
      "reason": "dependency-free by contract (runs on every prompt render) and byte-mirrored to hooks/statusline.mjs, so a relative import would resolve from two different directories; scripts/test-layout.mjs asserts its literals still match the layout"
    },
    {
      "file": "hooks/statusline.mjs",
      "reason": "byte-for-byte mirror of adapters/hooks/statusline.mjs, generated by scripts/gen-activation-assets.mjs"
    }
  ],
  "rules": [
    {
      "id": "JOIN_ONTO_STATE_DIR",
      "why": "a literal segment is joined onto stateDir; add an accessor in runtime/layout.mjs instead"
    },
    {
      "id": "LITERAL_STATE_DIR_IN_JOIN",
      "why": "the state directory is composed by hand inside a path call; use stateDir() from runtime/layout.mjs"
    },
    {
      "id": "LITERAL_STATE_SUBPATH",
      "why": "the state directory and a child are composed as literals; add an accessor in runtime/layout.mjs instead"
    },
    {
      "id": "LITERAL_STATE_SLASH_SUBPATH",
      "why": "a '.agent-sdlc/<child>' path literal; derive it from runtime/layout.mjs instead"
    }
  ],
  "known_bypasses": [
    "string concatenation",
    "template literal assembly",
    "a path constant defined in another module"
  ],
  "self_check": {
    "status": "PASS",
    "checks": 6
  },
  "violations": [],
  "status": "PASS"
}
