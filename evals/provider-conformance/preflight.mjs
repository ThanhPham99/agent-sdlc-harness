#!/usr/bin/env node
import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';import {probe,capabilities} from '../../runtime/provider.mjs';
const ROOT=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'../..');const hosts=['claude','codex','antigravity'];const results=hosts.map(host=>{const p=probe(host);return {...capabilities(host,p),status:p?'READY':'PENDING'};});const out={schema:'agent-sdlc/provider-preflight/v1',version:JSON.parse(fs.readFileSync(path.join(ROOT,'agent-sdlc.manifest.json'),'utf8')).version,results,qualified:false,note:'READY means CLI capability preflight only; it is not semantic qualification.'};fs.writeFileSync(path.join(ROOT,'evals','PROVIDER-PREFLIGHT.json'),JSON.stringify(out,null,2)+'\n');console.log(JSON.stringify(out,null,2));
