---
description: Grant approval for a paused SDLC human gate ticket and resume execution
---

Run `node "${CLAUDE_PLUGIN_ROOT:-.}/runtime/cli.mjs" auto --approve` (or `bin/agent-sdlc auto --approve`) to grant the pending human confirmation gate ticket and resume pipeline execution.
