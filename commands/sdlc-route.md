---
description: Route a software task into the canonical SDLC workflow without model inference
---

Run `node "${CLAUDE_PLUGIN_ROOT}/runtime/cli.mjs" route "$@"` to determine the exact workflow, risk profile, and required overlays for your objective.
