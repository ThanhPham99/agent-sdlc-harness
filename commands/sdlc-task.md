---
description: Manage and view the persistent task graph
---

Run `node "${CLAUDE_PLUGIN_ROOT:-.}/runtime/cli.mjs" task list` or `task progress` (or `bin/agent-sdlc task list`) to inspect active tasks and readiness.
