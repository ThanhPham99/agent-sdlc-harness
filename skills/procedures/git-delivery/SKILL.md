---
name: git-delivery
description: Manage branches, worktrees, PR lifecycle, merge queue, CI evidence validation, and post-merge reconciliation. Dispatched by sdlc-orchestrator for stage REVIEW, RELEASE. Not an entry point; do not activate from a user prompt.
metadata:
  version: "3.0.0-rc2"
---
<!-- GENERATED by scripts/gen-skill-surface.mjs from config/procedures.json. Do not edit by hand. -->
# Procedure: git-delivery

Applies at REVIEW, RELEASE, when `always` holds. `sdlc-orchestrator` lists the
procedures that apply through `navigation.procedure_skills`; this module is not
selected by preference.

The instructions are `harness/internal-skills/git-delivery.md`. Read that file. It is the single
source of this procedure's guidance — the compiled context inlines the same text,
so the two can never disagree.
