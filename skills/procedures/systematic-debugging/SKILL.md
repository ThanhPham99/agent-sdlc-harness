---
name: systematic-debugging
description: Isolate root causes with minimal reproduction, form bounded hypotheses, and verify fixes with regression tests. Dispatched by sdlc-orchestrator for stage IMPLEMENT, VERIFY. Not an entry point; do not activate from a user prompt.
metadata:
  version: "3.0.0-rc2"
---
<!-- GENERATED by scripts/gen-skill-surface.mjs from config/procedures.json. Do not edit by hand. -->
# Procedure: systematic-debugging

Applies at IMPLEMENT, VERIFY, when `bug_workflow` holds. `sdlc-orchestrator` lists the
procedures that apply through `navigation.procedure_skills`; this module is not
selected by preference.

The instructions are `harness/internal-skills/systematic-debugging.md`. Read that file. It is the single
source of this procedure's guidance — the compiled context inlines the same text,
so the two can never disagree.
