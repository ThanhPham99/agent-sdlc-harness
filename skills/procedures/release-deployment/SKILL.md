---
name: release-deployment
description: Execute release gates, package validation, provenance checks, deployment strategies, and rollback verification. Dispatched by sdlc-orchestrator for stage RELEASE, DEPLOY. Not an entry point; do not activate from a user prompt.
metadata:
  version: "3.0.0-rc2"
---
<!-- GENERATED by scripts/gen-skill-surface.mjs from config/procedures.json. Do not edit by hand. -->
# Procedure: release-deployment

Applies at RELEASE, DEPLOY, when `always` holds. `sdlc-orchestrator` lists the
procedures that apply through `navigation.procedure_skills`; this module is not
selected by preference.

The instructions are `harness/internal-skills/release-deployment.md`. Read that file. It is the single
source of this procedure's guidance — the compiled context inlines the same text,
so the two can never disagree.
