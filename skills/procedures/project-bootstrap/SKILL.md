---
name: project-bootstrap
description: Initialize project invariants, detect project topology, tools, conventions, and verify auto-activation state. Dispatched by sdlc-orchestrator for stage INTAKE, REQUIREMENTS. Not an entry point; do not activate from a user prompt.
metadata:
  version: "3.0.0-rc2"
---
<!-- GENERATED by scripts/gen-skill-surface.mjs from config/procedures.json. Do not edit by hand. -->
# Procedure: project-bootstrap

Applies at INTAKE, REQUIREMENTS, when `new_feature_without_project_knowledge` holds. `sdlc-orchestrator` lists the
procedures that apply through `navigation.procedure_skills`; this module is not
selected by preference.

The instructions are `harness/internal-skills/project-bootstrap.md`. Read that file. It is the single
source of this procedure's guidance — the compiled context inlines the same text,
so the two can never disagree.
