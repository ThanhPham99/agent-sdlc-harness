---
name: tdd
description: Follow red-green-refactor cycle: write minimal failing test first, implement minimal passing code, then refactor. Dispatched by sdlc-orchestrator for stage IMPLEMENT. Not an entry point; do not activate from a user prompt.
metadata:
  version: "3.0.0-rc2"
---
<!-- GENERATED by scripts/gen-skill-surface.mjs from config/procedures.json. Do not edit by hand. -->
# Procedure: tdd

Applies at IMPLEMENT, when `strict` holds. `sdlc-orchestrator` lists the
procedures that apply through `navigation.procedure_skills`; this module is not
selected by preference.

The instructions are `harness/internal-skills/tdd.md`. Read that file. It is the single
source of this procedure's guidance — the compiled context inlines the same text,
so the two can never disagree.
